---
---
<section class="hero-section">
  <div class="hero-background"></div>
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <h1 class="fade-in">Soluciones Integrales en Construcción e Ingeniería</h1>
    <p class="fade-in">Gestionamos tu proyecto de principio a fin con un servicio profesional, responsable y de la más alta calidad.</p>
    <a href="/contacto" class="button fade-in">Cotiza tu Proyecto Ahora</a>
  </div>
</section>
<style>
  .hero-section {
    position: relative;
    height: 90vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: var(--color-text-light);
  }
  .hero-background {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background-image: url('/images/hero-background.jpg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
  }
  .hero-overlay {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background-color: rgba(18, 18, 18, 0.6);
  }
  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 900px;
    padding: 0 1rem;
  }
  .hero-content h1 {
    color: #fff;
    font-size: 3.8rem;
    margin-bottom: 1rem;
  }
  .hero-content p {
    font-size: 1.3rem;
    margin-bottom: 2.5rem;
    opacity: 0.9;
  }
  .button {
    background-color: var(--color-primario);
    color: white;
    padding: 15px 35px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    transition: background-color 0.3s ease, transform 0.2s ease;
    box-shadow: var(--shadow-md);
  }
  .button:hover {
    background-color: var(--color-primario-hover);
    transform: translateY(-3px);
  }
  .hero-content h1 { transition-delay: 0.2s; }
  .hero-content p { transition-delay: 0.4s; }
  .hero-content .button { transition-delay: 0.6s; }
</style>